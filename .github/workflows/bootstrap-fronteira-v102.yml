name: Bootstrap Fronteira Iris V102

on:
  push:
    branches:
      - test/fronteira-iris-v102

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - name: Sanity check
        run: echo "workflow ok"
